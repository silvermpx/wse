{
  "name": "wse-client",
  "version": "1.0.0",
  "description": "React client for WSE (WebSocket Event System). Type-safe hooks with automatic reconnection.",
  "main": "client/index.ts",
  "types": "client/index.ts",
  "files": [
    "client/"
  ],
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest"
  },
  "keywords": [
    "websocket",
    "react",
    "hooks",
    "real-time",
    "events",
    "pubsub"
  ],
  "license": "MIT",
  "dependencies": {
    "pako": "^2.1.0",
    "@msgpack/msgpack": "^3.0.0"
  },
  "peerDependencies": {
    "react": ">=18",
    "zustand": ">=4"
  },
  "devDependencies": {
    "typescript": ">=5",
    "react": "^18.3.1",
    "zustand": "^4.5.0",
    "@types/react": "^18.3.0",
    "vitest": "^2.0.0"
  }
}
